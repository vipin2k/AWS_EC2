pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                // Your steps here
                sshPublisher(publishers: [sshPublisherDesc(configName: 'ec2-13-233-224-136.ap-south-1.compute.amazonaws.com', transfers: [sshTransfer(cleanRemote: false, excludes: '', execCommand: 'apt-get update', execTimeout: 120000, flatten: false, makeEmptyDirs: false, noDefaultExcludes: false, patternSeparator: '[, ]+', remoteDirectory: '', remoteDirectorySDF: false, removePrefix: '', sourceFiles: '*.war')], usePromotionTimestamp: false, useWorkspaceInPromotion: false, verbose: false)])
            }
        }
        // Add more stages if needed
    }
}
