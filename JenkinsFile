pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                // Your steps here
                sshPublisher(publishers: [sshPublisherDesc(configName: 'dev', transfers: [sshTransfer(cleanRemote: false, excludes: '', execCommand: 'ls -l', execTimeout: 120000, flatten: false, makeEmptyDirs: false, noDefaultExcludes: false, patternSeparator: '[, ]+', remoteDirectory: '/home/ubuntu/', remoteDirectorySDF: false, removePrefix: '', sourceFiles: 'C:\\\\Users\\\\Vipinraj\\\\.jenkins\\\\workspace\\\\EC2_main\\\\jenkins-keypair.pem')], usePromotionTimestamp: false, useWorkspaceInPromotion: false, verbose: false)])
            }
        }
        // Add more stages if needed
    }
}
