pipeline {
    agent any

    stages {
        stage('Connect to EC2 via SSH') {
            steps {
                sshagent(credentials : ['15.206.145.189']){
                sh "ssh command"

              } 
            }
        }
    }
}